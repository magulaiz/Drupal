services:
  taxonomy_term.breadcrumb:
    class: Drupal\taxonomy\TermBreadcrumbBuilder
    arguments: ['@entity_type.manager', '@entity.repository']
    tags:
      - { name: breadcrumb_builder, priority: 1002 }
  taxonomy_term.taxonomy_term_route_context:
    class: Drupal\taxonomy\ContextProvider\TermRouteContext
    arguments: ['@current_route_match']
    tags:
      - { name: 'context_provider' }
  taxonomy.permissions:
    class: Drupal\taxonomy\TaxonomyPermissions
    autowire: true
    tags:
      - { name: 'user.permission_provider', method: 'permissions', provider: 'taxonomy' }
  Drupal\taxonomy\TaxonomyPermissions: '@taxonomy.permissions'
