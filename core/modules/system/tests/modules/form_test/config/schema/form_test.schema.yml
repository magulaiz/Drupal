form_test.object:
  type: config_object
  label: 'Test form'
  mapping:
    bananas:
      type: string
      label: 'Bananas'
    favorite_vegetable:
      type: required_label
      label: 'Favorite vegetable'
      # Optional because different tests exercise different parts.
      # @see \Drupal\Tests\system\Kernel\Form\FormObjectTest
      # @see \Drupal\Tests\Core\Form\ConfigTargetTest
      requiredKey: false
    nemesis_vegetable:
      type: required_label
      label: 'Nemesis vegetable'
      # Optional because different tests exercise different parts.
      # @see \Drupal\Tests\system\Kernel\Form\FormObjectTest
      # @see \Drupal\Tests\Core\Form\ConfigTargetTest
      requiredKey: false
