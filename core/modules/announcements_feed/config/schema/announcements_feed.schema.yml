announcements_feed.settings:
  type: config_object
  label: 'Announcements Feed Settings'
  mapping:
    max_age:
      type: integer
      label: 'Cache announcements for max-age milliseconds.'
    cron_interval:
      type: integer
      label: 'Cron interval for fetching announcements in milliseconds.'
