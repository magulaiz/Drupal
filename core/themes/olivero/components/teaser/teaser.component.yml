$schema: https://git.drupalcode.org/project/drupal/-/raw/10.1.x/core/modules/sdc/src/metadata.schema.json
name: Teaser
status: experimental
props:
  type: object
  # required:
  #   - header
  properties:
    attributes: 
      type: Drupal\Core\Template\Attribute
    classes:
      type: array
    label:
      type: string
    content:
      type: object
    url:
      type: string
    title_suffix:
      type: array
    title_prefix:
      type: array
    page:
      type: boolean
    # header:
    #   type: string
    #   title: Header
    #   description: The teaser header
    #   examples:
    #     - I am a header!
slots:
  author: {}
  description: {}
    # title: Body
    # description: The contents of the card.
    # examples:
    #   - <p>Foo is <strong>NOT</strong> bar.</p>
