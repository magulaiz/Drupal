# This is the DrupalCI testbot build file for Drupal core.
# Learn to make one for your own drupal.org project:
# https://www.drupal.org/drupalorg/docs/drupal-ci/customizing-drupalci-testing
build:
  assessment:
    validate_codebase:
      container_command:
        commands:
          - "cd ${SOURCE_DIR} && sudo -u www-data composer require drupal/coder:8.3.x-dev"
    testing:
      # Run code quality checks.
      container_command.commit-checks:
        commands:
          - "core/scripts/dev/commit-code-check.sh --drupalci"
        halt-on-fail: true
      # Run no tests, we only want the coding standards output for now.
