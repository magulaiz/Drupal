# Deny remote requests in Apache 2.4+.
<IfModule mod_authz_core.c>
	Require local
</IfModule>

# Deny remote requests in Apache 2.0-2.2.
<IfModule !mod_authz_core.c>
	Order deny,allow
	Deny from all
	Allow from 127.0.0.1
</IfModule>
